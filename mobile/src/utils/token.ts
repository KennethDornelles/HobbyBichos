// src/utils/token.ts
export async function getToken(): Promise<string | null> {
  // TODO: Implemente a busca real do token (ex: AsyncStorage)
  return null;
}

export function decodeToken(token: string): any {
  // TODO: Implemente a decodificação real do token (ex: jwt-decode)
  return null;
}
